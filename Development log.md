创建时间：
20260420，19：37，GMT+5

## 待开发

- [ ] 预处理工具，主要是偏最小二乘法去基线，以及异常值去除（宇宙射线）。
  - 基于"Raman-mapping"库的去基线工具进行迁移修改。增加异常值去除功能。
  - 异常值去除思路：
    - 局部鲁棒参考曲线 → 残差标准化打分 → 宽度约束（最多2个像素） → 插值修复（以修复位置为中心的均值，避免出现连续相等的值，如果旁边点也需要补，中心窗不能用旁边的异常值去算均值）。
- [ ] 数据打标工具：用于手动给光谱打标签，用于制作基础的训练集。读取预处理后的物理数据，然后可以对其中部分光谱或者全部光谱打标签，没有手动标记的光谱都会被标记为”U“。输出是标记后的物理数据。